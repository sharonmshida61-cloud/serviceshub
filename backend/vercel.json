{
  "version": 2,
  "builds": [
    {
      "src": "src/index.js",
      "use": "@vercel/node",
      "config": {
        "maxDuration": 60,
        "includeFiles": "node_modules/.prisma/**"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/index.js" }
  ]
}
